
# TODO: workaround until fink/larix has native support for `find...:`
find = fn found: fn items: pipe items:
  flat_map item:
    [[item, true], [null, !found(item)]]
  while [item, continue]:
    continue
  fold [item], acc=null:
    item

